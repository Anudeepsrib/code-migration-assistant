[tool:pytest.ini]
minversion = 6.0
addopts = 
    --cov=skills/code_migration
    --cov-report=html
    --cov-report=term-missing
    --strict-markers
    --disable-warnings
    --timeout=300
    --tb=short

markers =
    security: Security-related tests
    compliance: Compliance scanning tests
    performance: Performance tests
    integration: End-to-end tests
    slow: Tests that take longer to run

testpaths =
    skills/code_migration/tests/security
    skills/code_migration/tests/compliance
    skills/code_migration/tests/integration
    skills/code_migration/tests/performance

filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

python_files = test_*.py
python_classes = Test*
python_functions = test_*
python_modules = test_*
