[tool:pytest.ini]
minversion = 6.0
addopts = 
    --cov=skills/code_migration
    --cov-report=html
    --cov-report=term-missing
    --strict-markers
    --disable-warnings
    --timeout=300
    --tb=short

markers =
    security: Security-related tests
    compliance: Compliance scanning tests
    performance: Performance tests
    integration: End-to-end tests
    slow: Tests that take longer to run

testpaths = [
    skills/code-migration/tests/security
    skills/code-migration/tests/compliance
    skills/code-migration/tests/integration
    skills/code-migration/tests/performance
]

filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

python_files = test_*.py
python_classes = Test*
python_functions = test_*
python_modules = test_*
